---
# role defaults retained for legacy/backwards compatibility;
# most service-specific settings now live in vars/main.yml instead.
# (vars/* has higher precedence than defaults/*.)

# empty for now, see roles/jenkins/vars/main.yml
